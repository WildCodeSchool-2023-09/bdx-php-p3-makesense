<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Raleway:wght@100;400&display=swap" rel="stylesheet">
        <link rel="icon" type="image/png" href="{{ asset('build/images/Makesense-logo.webp') }}"/>
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
        <title>
            {% block title %}Makesense{% endblock %}
        </title>
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}
        <header>
        </header>
        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        {% include '_navbar.html.twig' %}
        {% for flashMessage in app.flashes('success') %}
                <div class="alert alert-success" id="flash-message">{{ flashMessage }}</div>
            <script>
                // Cacher le message flash après 3 secondes
                setTimeout(function() {
                    document.getElementById('flash-message').style.display = 'none';
                }, 3000); // 3000 millisecondes = 3 secondes
            </script>
        {% endfor %}
        <main>
            {% block body %}
            {% endblock %}
        </main>
        <footer>
       {% include 'footer.html.twig' %}
        </footer>
    <script src="/assets/js/decisionAcordeon.js"></script>
    </body>
</html>
