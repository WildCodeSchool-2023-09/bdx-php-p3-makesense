
    {{ form_start(form) }}
<div class="container">
    <div class="my-card-box">
{#       <div class="txt">#}
          <div class="bloc">

                {{ form_row(form.text, {label: 'Ecrivez ici :'}) }}
          </div>
    <button type="submit" class="btn">valider</button>
    {{ form_end(form) }}

{#            </div>#}
     </div>
</div>
