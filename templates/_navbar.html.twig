<nav class="navbar">
    <div class="navbar-left">
        <ul>
            <li> <i class="fa-solid fa-house"></i> <a href="{{ path('app_decision_index') }}">Parcourir les décisions</a></li>
            <li> <i class="fa-regular fa-calendar-days"></i> <a href="{#{{ path('') }}#}">Mes décisions</a></li>
            <li> <i class="fa-regular fa-face-smile"></i> <a href="{#{{ path('') }}#}">Notifications</a></li>
            <li> <i class="fa-regular fa-square-plus"></i> <a href="{{ path('app_decision_new') }}">Créer une décision</a></li>
        </ul>
    </div>

    <div class="navbar-right">
        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <p><a href="{{ path('app_logout') }}">Me déconnecter</a></p>
            <a href="{{ path('app_user_show', {'id': app.user.id}) }}">
            <i class="fa-solid fa-user burger-menu"></i>
            </a>
        {% else %}
            <p><a href="{{ path('app_login') }}">Me connecter</a></p>
            <i class="fa-solid fa-user burger-menu"></i>
        {% endif %}
    </div>
</nav>
